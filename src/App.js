import React from 'react';
import ColorPicker from './ColorPicker';

class App extends React.Component {
  constructor(props) {
    super(props);
  }

  render() {
      return (
        <ColorPicker/>
      );
  }
}

export default App;
